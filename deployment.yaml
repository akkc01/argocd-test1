apiVersion: apps/v1
kind: Deployment
metadata:
  name: akkc-deployment
  labels:
    app: akkc-portfolio-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: akkc-portfolio
  template:
    metadata:
      labels:
        app: akkc-portfolio
    spec:                            # 👈 Correct place for this spec
      containers:
      - name: nginx
        image: akkc01/akkc:v1
        ports:
          - containerPort: 80
        # envFrom:
        #   - configMapRef:
        #       name: my-config       # Your config map with env vars
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "200m"
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html
        configMap:
          name: dewa-html           # HTML content from ConfigMap


